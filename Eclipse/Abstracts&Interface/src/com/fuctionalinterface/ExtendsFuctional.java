package com.fuctionalinterface;


interface IDiscountApp{
	
	void doSomething(int a, double b);
}


// if empty its fuctional interface is overrideed the same method than also fuctional if we write other method other than same override method than its becomes normal methods
interface IAnotherDiscount extends IDiscountApp{
	
	
	
}


public interface ExtendsFuctional {

}
